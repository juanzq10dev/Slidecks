[project]
name = "api-examples"
version = "0.1.0"
description = "Ejemplos simples de consumo de APIs públicas con HTTP + JSON"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "ipykernel>=6.29",
    "notebook>=7.2",
]
