[project]
name = "http-detective-client"
version = "0.1.0"
description = "Cliente del estudiante para el juego HTTP Detective"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "ipykernel>=6.29",
    "notebook>=7.2",
]
